# bi-modernization — dependencies
# Install: pip install -r requirements.txt
#
# Covers both halves of the skill: source extraction (BI file -> inventory /
# semantic YAML) and generation (inventory -> Streamlit / React / agent).

# ── Source parsing ─────────────────────────────────────────────────────────
# LookML parser (official Looker package)
lkml>=1.3.1

# XML bomb protection for Tableau TWB/TDS parsing
defusedxml>=0.7.1

# Power BI .pbix parsing (xpress9 ships precompiled wheels — no C compiler needed)
pbixray>=0.1.0

# HTTP client for REST APIs (Tableau, Looker, Power BI, BO, Denodo Catalog)
requests>=2.28.0

# ── Output generation ──────────────────────────────────────────────────────
# Semantic View YAML
pyyaml>=6.0

# Dashboard generation
plotly>=5.22.0
pandas>=2.0.0
jinja2>=3.1.3

# Excel / HTML reporting
openpyxl>=3.1.2

# ── Snowflake connectivity (for --execute flags) ────────────────────────────
snowflake-connector-python>=3.6.0

# ── Optional ───────────────────────────────────────────────────────────────
# Visual QA comparison (scripts/visual_compare.py). Laptop-only: this never runs
# inside Streamlit in Snowflake, so it is not a deployment dependency.
# pillow>=10.0.0
#
# JDBC bridge for Denodo VDP (live VDP connections only; requires a Java runtime)
# jaydebeapi>=1.2.3
