# Core parsing (shared with semantic-extraction)
lkml>=1.3.1
defusedxml>=0.7.1

# Dashboard generation
plotly>=5.22.0
pandas>=2.0.0
jinja2>=3.1.3

# Excel/reporting (already in semantic-extraction env)
openpyxl>=3.1.2

# Snowflake connectivity (for --execute flag)
snowflake-connector-python>=3.6.0
